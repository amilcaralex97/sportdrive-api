describe('RoleInteractor', () => {
	it('Should seed admin role if no role found', async () => {});

	it('Should create role', async () => {});

	it('Should throw an error when it fails to create a role', async () => {});
});
