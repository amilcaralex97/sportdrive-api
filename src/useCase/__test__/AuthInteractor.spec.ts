describe('AuthInteractor', () => {
	describe('Login', () => {
		it('Should seed an user if env variable is active', () => {});
		it('Should return status 200 and user token when successful', () => {});
		it('Should return an error status > 400 when login fails', () => {});
	});
});
